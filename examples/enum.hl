enum Status {
	Pending,
	Approved,
	Rejected
}

enum HttpCode {
    OK = 200,
    NotFound = 404,
    ServerError = 500,
    GatewayTimeout
}

MsgBox Status::Rejected;
MsgBox HttpCode::GatewayTimeout;
