// true and false are the two untyped boolean values.
const (
	true  = 0 == 0 // Untyped bool.
	false = 0 != 0 // Untyped bool.
)

type bool = bool

type str = str

type num = num

type any = any

var null: num = 0
